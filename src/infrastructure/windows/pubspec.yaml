name: infrastructure_windows
description: Windows-specific implementations for WHPH infrastructure layer.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.5.3
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies
  application:
    path: ../../core/application
  domain:
    path: ../../core/domain
  infrastructure_shared:
    path: ../shared
  acore:
    path: ../../../packages/acore-flutter

  # Windows-specific dependencies
  audioplayers: ^6.5.1
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
