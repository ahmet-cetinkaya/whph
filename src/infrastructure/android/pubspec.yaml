name: infrastructure_android
description: Android-specific implementations for WHPH infrastructure layer.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.5.3
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies
  application:
    path: ../../core/application
  domain:
    path: ../../core/domain
  acore:
    path: ../../../packages/acore-flutter

  # Android-specific dependencies
  android_intent_plus: ^5.3.0
  share_plus: ^12.0.1
  home_widget: ^0.6.0
  file_saver: 0.2.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
