name: application
description: Application layer for WHPH containing use cases (CQRS commands/queries) and business logic services.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.5.3
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies
  domain:
    path: ../domain

  # External dependencies
  acore:
    path: ../../../packages/acore-flutter
  mediatr: ^0.2.1
  equatable: ^2.0.7
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
