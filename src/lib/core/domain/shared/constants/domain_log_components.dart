/// Log component identifiers for core layer structured logging.
///
/// These constants provide consistent component names for use with
/// the Logger.* methods' `component` parameter.
class DomainLogComponents {
  DomainLogComponents._();

  // Domain
  static const String task = 'Task';
}
